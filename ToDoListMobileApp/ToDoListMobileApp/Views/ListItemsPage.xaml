<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoListMobileApp.Views.ListItemsPage">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="ListViewItems" ItemsSource="{Binding Items}" HasUnevenRows="True" VerticalOptions="FillAndExpand">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10">
                                <Label x:Name="LabelTitle" Text="{Binding Title}" TextColor="Black" FontSize="16" LineBreakMode="NoWrap"/>
                                <Label x:Name="LabelDescription" Text="{Binding Description}" TextColor="Gray" FontSize="13" LineBreakMode="NoWrap"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button x:Name="ButtonAddItem" Text="Add Item" Clicked="ButtonAddItem_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>